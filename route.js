import {NextResponse} from 'next/server';
const SOURCE='https://nt.studybeepro.site/batches.json';
function n(x){if(!x||typeof x!=='object')return null;const id=x.id??x.course_id??x.courseId;if(id==null)return null;return {id:String(id),title:x.title??x.name??'Untitled Batch',description:x.description??'',thumbnail:x.thumbnail??x.image??x.banner??'',mrp:x.mrp??'',offer_price:x.offer_price??x.price??'',price:x.price??x.offer_price??'FREE',is_new:x.is_new??'',is_trending:x.is_trending??'',start_date:x.start_date??'',end_date:x.end_date??''}}
function ex(j){let a=[];if(Array.isArray(j))a=j;if(j&&typeof j==='object'){for(const k of ['new','courses','batches'])if(Array.isArray(j[k]))a.push(...j[k]);if(Array.isArray(j.data))a.push(...j.data);if(Array.isArray(j.data?.courses))a.push(...j.data.courses);if(Array.isArray(j.data?.batches))a.push(...j.data.batches)}const s=new Set();return a.map(n).filter(Boolean).filter(x=>!s.has(x.id)&&s.add(x.id))}
export async function GET(){try{const r=await fetch(SOURCE,{cache:'no-store',headers:{accept:'application/json'}});if(!r.ok)return NextResponse.json({success:false,error:`Batch source returned ${r.status}`},{status:502});return NextResponse.json({success:true,batches:ex(await r.json())})}catch{return NextResponse.json({success:false,error:'Unable to load batches.'},{status:502})}}
